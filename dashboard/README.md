# Public dashboard (Dune)

Principle: a metric without its failure context is marketing; a metric with
failures included is evidence.

This directory versions the public dashboard's SQL queries:
- total executions and per route (INCLUDING reverts)
- revert rate (30d)
- gas spent vs. aggregate net result

TODO: add queries after the first testnet deployment and link the dashboard
from the root README.
