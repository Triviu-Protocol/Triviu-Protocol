## What this PR does

## Checklist

- [ ] `forge build && forge test` pass (if touching `contracts/`)
- [ ] `npm run typecheck` passes (if touching `engine/`)
- [ ] Ran on a local fork first (if touching execution)
- [ ] Architecture decision? Tradeoff Record created/linked — with a COSTS line
- [ ] Docs / public-facing? Brand Manual § 9.4 checklist applied
      (zero income promises · risk notice · AI label · no "trilemma solved")
